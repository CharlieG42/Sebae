# ============================================================
# Configuration de MyHydraulicApp
# ============================================================

[app]
name    = "Sebae"
version = "0.2.0"

# ------------------------------------------------------------
# Base de données
# type : "sqlite" | "access"
# path : chemin vers le fichier (relatif au dossier du projet)
# ------------------------------------------------------------
[database]
type = "sqlite"
path = "sebae.db"

# ------------------------------------------------------------
# Compte administrateur
# Pour générer un hash SHA-256 :
#   python -c "import hashlib; print(hashlib.sha256('monMotDePasse'.encode()).hexdigest())"
# Valeur par défaut : login=admin / mdp=admin  (à changer en production)
# ------------------------------------------------------------
[admin]
login        = "admin"
password_sha = "8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918"
